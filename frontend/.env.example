# Optional. Leave unset for local dev (uses Vite's /api proxy to localhost:4000).
# In production, set to the deployed backend's URL + /api, e.g.:
# VITE_API_BASE_URL=https://crypto-advisor-backend.onrender.com/api
VITE_API_BASE_URL=
